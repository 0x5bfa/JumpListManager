<ContentDialog
	x:Class="JumpListManager.Views.CrcCalculatorDialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Title="Calculate CRC hash"
	PrimaryButtonText="OK"
	Style="{StaticResource DefaultContentDialogStyle}"
	mc:Ignorable="d">

	<Grid>

		<StackPanel Spacing="12">

			<TextBox
				IsSpellCheckEnabled="False"
				KeyUp="TextBox_KeyUp"
				PlaceholderText="Enter AUMID"
				TextChanged="TextBox_TextChanged" />

			<FontIcon Foreground="{ThemeResource TextFillColorSecondaryBrush}" Glyph="&#xE74B;" />

			<TextBox IsReadOnly="True" Text="{x:Bind ViewModel.CrcHash, Mode=OneWay}" />

		</StackPanel>


	</Grid>
</ContentDialog>
